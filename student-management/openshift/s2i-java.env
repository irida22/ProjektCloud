# Variabla per OpenShift Java S2I (nese perdorin oc new-app me builder Java)
# Vendos ne BuildConfig ose: oc set env bc/<emri> --from-env-file=s2i-java.env

MAVEN_ARGS=-pl web -am -DskipTests package
ARTIFACT_DIR=web/target
JAVA_APP_JAR=student-management.jar
