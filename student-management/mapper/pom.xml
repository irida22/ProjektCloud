<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.student.management</groupId>
        <artifactId>student-management</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>

    <artifactId>mapper</artifactId>
    <name>mapper</name>
    <description>MapStruct mappers between Entity and DTO</description>

    <dependencies>
        <dependency>
            <groupId>com.student.management</groupId>
            <artifactId>model</artifactId>
        </dependency>
        <dependency>
            <groupId>com.student.management</groupId>
            <artifactId>dto</artifactId>
        </dependency>
        <dependency>
            <groupId>org.mapstruct</groupId>
            <artifactId>mapstruct</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-context</artifactId>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <!--
      MapStruct: kontrata në StudentMapper (@Mapper).
      Implementimi në src/.../StudentMapperImpl.java (jo në target/).
      Çaktivizohet mapstruct-processor këtu që IDE të mos analizojë skedarë të gjeneruar në target/.
    -->
    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <annotationProcessorPaths combine.self="override">
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>${lombok.version}</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
